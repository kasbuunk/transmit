(
	log_level: "debug",
	transmitter: Nats(
		Config(
			host: "localhost",
			port: 4442,
		),
	),
	repository: Postgres(
		Config(
			name: "transmit",
			host: "localhost",
			port: 5432,
			user: "postgres",
			password: "postgres",
			ssl: false,
		),
	),
	transport: Grpc(
		Config(
			port: 8080,
		),
	),
)
